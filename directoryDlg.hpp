int directoryDlg (	HWND		owner,
					unsigned	dialogID,
					char		*directory,
					char		*dialogTitle
					);
int	conditionalDirectoryDlg (
					HWND		owner,
					unsigned 	dialogID,
					char		*directory,
					char		*dialogTitle
					);
